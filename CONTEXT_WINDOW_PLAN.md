# 上下文窗口与提示词裁剪方案

状态：已实施（2026-09-09）。日期：2026-09-09。

## 1. 背景

未提交改动把上下文策略改成了「不裁剪」：

- `src/shared/context-compiler.ts`：`DEFAULT_CONTEXT_BUDGET_TOKENS = Infinity`，并删除近期章节、滚动章纲、事实、文风样本、导演备注的全部 `.slice()` 上限。
- `electron/ai-service.ts:1092`：规划提示词把**全部**规划节点和**全部**已有章节写进每批请求（原为 `project.chapters.slice(-20)`）。

用真实代码路径加合成项目实测（1200 章 / 300 万字，使用仓库自带的 `estimateTextTokens`）：

| 指标 | 实测值 |
| --- | --- |
| 开书包最后一批提示词（已有 1190 章） | 约 45.4 万 token |
| 开书包全程累计输入（120 批） | 约 2,727 万 token |
| 单章生成上下文（第 1 章，设定齐全） | 约 11 万 token |
| 单章上下文构成（第 600 章） | 滚动章纲 6.7 万字符、契约 3.4 万字符、近期摘要 3.1 万字符 |

结论：1M 窗口模型单批装得下，但（a）128k/256k 模型与本地模型在 84 万～168 万字之间必然报错；（b）累计输入是平方级，成本与串行耗时随书长放大；（c）把 1200 章章纲平铺进上下文，长文召回质量下降。

作者已确认方向：**不需要给那么多正文，可以再往回收**。

## 2. 目标

1. 让上下文与提示词规模由**所选模型的上下文窗口**决定，而不是固定值或无限。
2. 大窗口模型不损失能力：结构、契约、当前卷、近窗章纲照给，只是不再重复给全书明细。
3. 小窗口模型和本地模型可用：超预算时按优先级降级，而不是直接 400。
4. 默认不发送章节正文；文风只用统计值，且只统计近期定稿。
5. 不改动人工门禁、质量硬门禁、检索逻辑和摘要生成逻辑。
6. 作者只选定方向并确认一次创作包，全书粗纲一次生成、近端章纲自动续跑，不需要按卷手动触发。

## 3. 设计

### 3.1 模型窗口的来源

`contextWindow` 已有完整链路，只是没接通：类型 `src/shared/ai/types.ts:41`、目录 `src/shared/ai/catalog.ts:24`、数据库列 `electron/database.ts`（`context_window`）、仓库 `electron/repositories/ai-profile-repository.ts:239`。目前 `electron/handlers/ai-profile-handlers.ts:168/333` 一律写入 `null`。

接通顺序：

1. 模型目录补全常见模型的窗口值（`catalog.ts` 的 `contextWindow`）。
2. 探测/模型列表结果可用时以探测值为准。
3. 设置页允许作者手动覆盖（本地模型建议填写）。
4. 未知时的默认值分两类：
   - **云端点：128k**。现代云模型的下限已到 128k，没有必要为不存在的 32k 模型牺牲装填量。
   - **本地端点：32k**。`electron/ai/transport.ts:38` 已通过 `local` 标记区分本地端点（`config.localEndpoint`），本地模型受显存限制，32k 是常见上限，按云端点处理只会白白多一次超长失败。
5. 注意：窗口值只是上限，不是目标。各段硬上限（见 3.3）把提示词约束在约 4–5 万 token，因此 1M 窗口也不会被填满——填满只会增加成本并降低长文召回质量。

### 3.2 预算公式

```
固定开销 = 系统提示 + 任务模板 + 输出 schema 的估算 token
输出预留 = maxOutputTokens（目录值，未知按 8k）
安全余量 = 2k（估算器误差可能 ±50%）
inputBudget = max(8k, (contextWindow - 输出预留 - 固定开销 - 安全余量) × 0.75)
```

0.75 系数用于吸收估算误差；仓库估算器对 JSON 偏保守（标点按 1/3、数字按 1/4 计），真实分词器更高，不能按 1.0 使用。

由于各段有硬上限，1M 窗口下的实际提示词仍在 4–5 万 token 量级，预算公式只在窗口较小（≤64k）时才真正起约束作用。

### 3.3 装填顺序与各段配额

不再用「先装完再按 TRIM_ORDER 整段删」的做法，改为**按优先级装填，装到预算为止**。同一段内部也按配额截断，并在段末注明省略数量。

| 优先级 | 段 | 配额与规则 |
| --- | --- | --- |
| 1 | 本章任务 | 全量（很小） |
| 2 | 契约核心字段 | 全量：前提、主角欲望、读者承诺、核心情绪、终局、不可变规则、禁写项、审美设定、自由度档位 |
| 3 | 结构层 | 宏观阶段 + 分卷全量（通常十几条） |
| 4 | 当前卷 | 全量：卷目标、矛盾、结果 |
| 5 | 近期章纲 | 最近 K 章，K 由剩余预算推导，下限 5、上限 100 |
| 6 | 滚动章纲 | 当前章之后 N 章，N 由剩余预算推导，下限 5、上限 120 |
| 7 | 期待账本 | 本章承接全量 + 已到期/逾期全量 + 其余待处理按配额 |
| 8 | 相关事实 | 沿用检索上限 80 条 |
| 9 | 长期记忆 | 已有 `buildLongTermMemory` 预算（全书/分卷/十章阶段摘要，上限约 1.2 万字符） |
| 10 | 人物与设定明细 | 见 3.4 |
| 11 | 文风统计 | 见 3.5 |
| 12 | 导演备注 | 最近 M 条，M 由剩余预算推导，上限 20（恢复原上限） |

预期结果（1200 章项目）：1M 窗口下 K≈100、N≈120；128k 窗口下 K≈20、N≈30；32k 窗口下 K≈5、N≈5。

### 3.4 契约长列表的裁剪

`keyRelationships`、`worldRules`、`majorForces`、`timelineAnchors`、`genreSpecificSections[].items`（人物档案、地理秩序、主线伏笔）可能各上百条。

v1（本方案）：

- 在预算内按顺序保留，超出时截断并在段末标注「另有 M 条已省略，可在故事圣经查看」。
- 优先保留与本章有共同专名的条目：以章纲、本章承诺、预期回报、危机、章末期待、当前卷目标为匹配文本，做人名/地名/势力名的子串匹配（专名表由人物档案首段与势力/关系条目提取，无需新依赖）。
- 未命中的条目在预算不足时先省略。

v2（后续可选）：作者在故事圣经里为条目加「常驻」标记，常驻条目永不被裁剪。

### 3.5 正文与文风

- **默认不发送任何章节正文**。当前 `styleSamples` 只用于统计（`authorStyle` 输出的是平均句长等数值，不注入正文），但仍会扫描全部定稿正文；改为只取最近 20 章定稿做统计，避免长篇每次生成都全量扫正文。
- `suggestAestheticProfile` 的「本书定稿样本」维持最近 6 章，但改为按预算截断（`electron/ai-service.ts:1504`）。
- 质检、润色、审稿等确实需要正文的任务照常发送目标章节，不受本方案限制。

### 3.6 开书包范围（方案 C）与自动续跑

作者选定方向后，其余交给 AI，不需要按卷手动触发。粒度分层：

| 层级 | 生成时机 | 覆盖范围 |
| --- | --- | --- |
| 宏观阶段 + 分卷 | 开书时一次生成 | 全书 |
| 粗纲（每 10 章 1 条） | 开书时一次生成 | 全书（1200 章约 120 条） |
| 章纲（细纲 + 场景卡） | 自动续跑 | 当前写作位置 + 前瞻 100 章 |

自动续跑机制（全部在后台，作者无感）：

- 触发点：开书确认后启动；每章保存/定稿后检查一次（复用 `electron/handlers/ai-handlers.ts:296` 已有的保存后钩子）；应用启动时检查一次。
- 续跑规则：从第一个缺章号开始，按 10 章一批补齐，直到「已备章纲 ≥ 当前写作位置 + 100」或到达全书末章；到末章自动停。
- 与正文生成互斥：沿用 `isGenerationActive` 判断，正文生成中不抢跑。
- 失败处理：沿用「已暂停 + 错误原因 + 一键继续」（`launchPack` 状态已有）。
- 门禁不变：自动生成的章纲仍是草稿，作者确认创作包这一步保留；写作时自然复核，符合 AGENTS.md「AI 只产出草稿」。

这样远处有粗纲定方向、近处有细纲可写，章纲按当时的契约和事实生成，不会像一次性生成全书细纲那样到第 800 章就过时。成本也从开书一次性 2,727 万 token 降到「结构 + 全书粗纲 + 第一卷章纲」几十万 token，之后按批滚动。

### 3.7 滚动规划路径

`electron/ai-service.ts` 的 `shared` 模板改为：

- 结构批次：契约 + 题材 + 目标字数 + 结构参数（本就很小）。
- 粗纲批次：契约 + 结构层全量 + 当前卷 + 分卷摘要。
- 章纲批次：契约 + 结构层全量 + 当前卷 + 最近 K 章章纲 + 更早内容的摘要（全书/分卷/十章阶段）+ 本卷内已完成批次的粗纲。
- 不再发送：全书规划节点列表、全书章节列表。
- 去重：细纲/场景卡与章节 outline 信息重复，同一批只保留一份；场景卡只带当前窗口。
- 保留现有的「生成期间契约/目标字数被修改则中止」和批次完整性校验，这两条是正确性保障。

按此口径估算，1200 章开书包的最后一批从约 45 万 token 降到窗口预算以内，累计输入从约 2,727 万 token 降到百万级。

### 3.8 超长兜底

供应商返回上下文超长错误时，捕获后把预算减半重试一次；仍失败才报错。这样即使窗口值填错或估算偏差，也不会直接失败。

### 3.9 可观测

`src/shared/context-diagnostics.ts` 增加：本次估算 token、所用窗口、预算、被裁剪的段与条目数。设置页在窗口未知时显示提示。创作包面板显示「已备章纲 N / 目标 M」与自动续跑状态。

## 4. 验收标准

1. 新增单测：1200 章项目在 32k / 128k / 1M 三档窗口下，`compileChapterContext` 的估算 token 均不超过 `inputBudget`；开书包最后一批提示词不超过预算。
2. 新增单测：开书包累计输入 token 相比当前基线下降一个数量级（记录基线值，防止回退）。
3. `tests/capacity.scale.test.ts` 增加一条「编译第 1/600/1200 章上下文且在预算内」的断言。
4. `npm run test:quality` 不回归（AGENTS.md 规则 6）。
5. 新增上下文相关用例需同步更新 `tests/context-compiler.test.ts` 中「无固定上限」的既有断言——该断言当前写死了不裁剪行为。
6. 自动续跑用例：已备章纲不足前瞻窗口时自动补齐到「当前章 + 100」；已存在章号不重复生成；正文生成进行中不抢跑；到达全书末章停止。

## 5. 不做

- 不改人工确认门禁（契约审批、创作包确认、定稿）。
- 不改质量硬门禁、检索排序、摘要生成。
- 不新增 AI 调用（压缩历史复用已有摘要层）。
- 不为省 token 而丢弃章节承接信息；承接信息通过近窗章纲 + 摘要双层保证。

## 6. 实施顺序

| 步骤 | 改动文件 | 内容 |
| --- | --- | --- |
| 1 | `src/shared/ai/catalog.ts`、`electron/handlers/ai-profile-handlers.ts`、设置页 | 接通 `contextWindow`，补目录值，支持手动覆盖 |
| 2 | `src/shared/context-compiler.ts` | 预算计算、按优先级装填、各段配额、正文与文风调整 |
| 3 | `src/shared/context-diagnostics.ts` | 窗口与裁剪诊断 |
| 4 | `electron/ai-service.ts` | 规划/开书包提示词窗口化；审美建议样本按预算 |
| 5 | `electron/handlers/project-handlers.ts`、`electron/main.ts` | 开书包范围改为方案 C；章纲自动续跑（保存后钩子 + 启动检查 + 前瞻 100 章） |
| 6 | `electron/ai-service.ts` | 上下文超长时减半重试一次 |
| 7 | `tests/` | 三档窗口用例、累计 token 基线、自动续跑、scale 断言 |
| 8 | `PROMPT_GUIDANCE_OPTIMIZATION_PLAN.md` 相关说明 | 同步文档 |

## 7. 风险

| 风险 | 对策 |
| --- | --- |
| 裁剪掉本章关键设定 | 专名命中优先保留；摘要兜底；诊断可见；后续可加作者「常驻」标记 |
| 估算误差导致仍超长 | 0.75 安全系数 + 超长减半重试一次 |
| 窗口值填错或未填 | 云端点默认 128k、本地端点默认 32k；超长时减半重试一次；设置页可覆盖 |
| 大窗口模型被过度裁剪 | 配额上限随窗口放大（1M 档近窗 100 章、前瞻 120 章），结构层与当前卷始终全量 |
| 与「完整创作包」功能冲突 | 开书包改为方案 C：结构 + 全书粗纲 + 第一卷章纲一次生成，其余章纲自动续跑；不再开书即生成全书细纲 |

## 8. 实施结果（2026-09-09）

### 已落地

| 项 | 实现 |
| --- | --- |
| 预算模块 | 新增 `src/shared/context-budget.ts`：云端点默认 128k、本地端点 32k，预算 =（窗口 − 输出预留 − 固定开销 − 余量）× 0.75 |
| 窗口来源 | `electron/ai/route-resolver.ts` 从探测/作者填写的能力行取 `contextWindow`，其次模型目录，最后按来源类型默认 |
| 单章上下文 | `context-compiler.ts` 按预算收缩各段配额：近窗摘要 5–100 章、滚动章纲 5–120 章、事实 10–80 条、待处理期待 5–40 条、契约列表 3–40 条、导演备注 3–20 条、长期记忆 1.8k–12k 字符；文风统计只看最近 20 章定稿；默认不发送章节正文 |
| 场景卡定位 | 改用父细纲的 ordinal 反推章号，`ordinal % 10` 仅作兜底 |
| 规划提示词 | `planningContext`：结构层 + 当前卷 + 本卷粗纲 + 近 60 章章纲 + 摘要；批次内只带紧邻的 20 章章纲 |
| 开书包范围 | 方案 C：结构 → 全书粗纲（每 10 章一条，一次请求最多 100 章）→ 开局 100 章章纲 |
| 自动续跑 | 章纲不足「当前写作位置 + 100」时后台补齐；触发点为保存章节后与打开项目时；与正文生成互斥；已确认的创作包才续跑 |
| 超长兜底 | 规划路径捕获上下文超长错误后把预算减半重试一次（已落盘批次不重试） |
| 诊断 | `ContextDiagnostics` 新增 `windowTokens` / `budgetTokens`，警告里显示预算与近窗/前瞻规模 |

### 实测（1200 章 / 300 万字项目）

| 指标 | 改造前 | 改造后 |
| --- | --- | --- |
| 开书包最后一批提示词 | 约 45.4 万 token | 约 1.35 万字符（近 60 章窗口 + 结构 + 摘要） |
| 开书包全程累计输入 | 约 2,727 万 token | 结构 + 120 条粗纲 + 100 章章纲，约百万 token 以内 |
| 单章上下文 | 11 万 token（全书大纲 + 全量摘要） | 受预算约束，128k 档约 4–5 万 token |

### 验证

- `npm test`：715 用例（714 通过 / 1 跳过，跳过项为默认关闭的联网样本测试）。
- `npm run test:quality`：20 通过，未回归。
- `npm run test:scale`：1 通过（约 102 秒，含第 1/600/1200 章上下文预算断言）。
- `npm run test:e2e`：22 通过。
- `npm run build`：通过。
- `npx biome check`：改动文件无告警。
- 新增用例：三档窗口（32k/128k）、小窗口收缩、不注入正文、粗纲模式、长篇提示词有界（<60k 字符）、自动续跑与未确认不续跑、续跑标志清理、父节点重指向、容量测试内上下文预算断言。

### 后续调整（2026-09-09）

- 云端点默认窗口由 128k 调整为 **1M**（`DEFAULT_CLOUD_CONTEXT_WINDOW = 1_000_000`）；本地端点仍为 32k，探测结果与作者填写值优先于默认值。
- 各段配额缩放的校准基准单独固定为 128k（`CONTEXT_REFERENCE_WINDOW`），因此改默认值不会让已探测或已填写的小窗口模型被额外压缩；`DEFAULT_CONTEXT_BUDGET_TOKENS` 改为按 1M 推导（约 73.8 万 token）。
- 实际装填量仍由各段硬上限约束（约 4–5 万 token 量级），1M 窗口不会被填满；改默认值的主要效果是超长契约不再触发整段裁剪。

### 已知限制

- 上下文超长减半重试目前只覆盖规划/开书包路径；正文生成路径的上下文由调用方编译，超长时给出明确报错，暂不自动重编。
- 契约长列表 v1 为「预算内截断 + 标注省略条数」，未做专名命中筛选（v2）。
- 全书粗纲是每 10 章一条的批次目标，不含章节级目录；章节级细节由逐章章纲与自动续跑负责。
