# 功能检查报告

检查日期：2026-09-09。对象：当前源码、已有打包文件、Windows 实际安装版和默认真实工作区。

## 结论

**不能判定当前安装版所有功能均可正常使用。** 最新源码的自动化检查通过，但真实工作区存在缺失结构，实际安装版尚未包含本次检查期间其他操作加入的数据库迁移与 AI 严格 JSON Schema 修复。

本次检查未修改真实工作区、凭据或安装文件。源码中的修复属于检查期间已有的并发改动，本报告不将这些改动归为本次检查所做。

## 使用阻塞点

| 问题 | 已验证的证据 | 影响 |
| --- | --- | --- |
| 真实目录库缺失历史迁移结构 | 以只读方式检查默认工作区：`user_version = 9`，但没有 `incubations` 表、`projects.words_per_chapter` 列或 `schema_migrations` 表。SQLite `quick_check` 为 `ok`，目录中有 3 本作品。 | 立项草稿、新建作品、项目设置和章节保存可能因缺表或缺列失败。应用日志已有对应错误。 |
| 已安装版未包含 AI 严格 schema 修复 | `E:\小说系统\fanqie-novel-studio\resources\app.asar` 的版本为 0.6.1；其中没有当前源码的 `toStrictJsonSchema` 修复。实际日志在 2026-09-09 11:16（北京时间）仍记录 `Required properties must match all properties`。 | AI 扩展开书方案等带可选字段的结构化输出任务可能返回 400；当前源码回归通过不代表已安装版可用。 |
| 打包冒烟测试对象与实际安装版不同 | `release/win-unpacked` 仍是 0.2.2，测试只检查其首页和 preload API；Windows 注册表指向 E 盘的 0.6.1。 | 仓库内打包应用的冒烟通过，不能证明实际安装文件已更新或其完整工作流正常。 |

真实工作区路径：`C:\Users\橙汁\Documents\长篇创作工作台数据`。

## 执行结果

| 检查 | 结果 | 说明 |
| --- | --- | --- |
| `npm test` | 675 通过，1 跳过 | 最新源码；包含新增迁移和严格 schema 回归。跳过项为默认关闭的联网样本测试。 |
| `npm run test:quality` | 20 通过 | 提示词结构、质量固定基准、题材、事实约束、上下文诊断和段落差异。计数与完整测试有重叠。 |
| `npm run test:e2e` | 22 通过 | 11 个场景分别运行桌面及移动视口；浏览器使用演示 API 和 localStorage。 |
| `npm run test:electron` | 2 通过 | 最新生产构建的 SQLite/IPC 工作流，以及仓库内旧打包程序启动。 |
| `npm run test:scale` | 1 通过，约 124 秒 | 10 本作品，每本 1,500 章、300 万字；验证数据隔离、列表/概览读取和单章正文。 |
| `npm run build` | 通过 | TypeScript、Vite、tsup 均成功；有大于 500 kB 的前端 chunk 提示。 |
| `FANQIE_LIVE_TEST=1` 下单独运行联网样本测试 | 1 通过 | 实际读取测试书目的前 10 章公开样本；不能推广为所有书目或完整榜单均可采集。 |

首次容量运行恰逢迁移接口与调用方被并发更新，报 `migration.run is not a function`。待改动一致后重跑通过，并重新执行了完整测试、构建及 Electron 冒烟。通过计数属于最后核验的源码状态。

## 按功能核对

| 功能 | 本次验证及边界 |
| --- | --- |
| 多书总览、项目创建和设置 | 浏览器交互、数据库集成、Electron 创建作品通过；真实旧目录库缺列仍会阻塞已安装版。 |
| 市场研究、榜单 CSV、样本导入、商业知识 | CSV/导入/研究规则和页面测试通过；一本书的公开前 10 章联网采集通过。 |
| 立项孵化、候选对比、开书包 | 浏览器交互和服务回归通过；浏览器候选是固定演示数据，真实安装版存在上述 AI 和缺表问题。 |
| 创作契约、阶段/卷/章规划 | 规则、审批保护、规划修复及 Electron 工作流通过；没有现场调用云模型生成规划。 |
| 写作、自动保存、版本、检索和上下文 | 浏览器自动保存、离开再返回、版本提示和检索通过；数据库保存与历史规则通过。 |
| 单章 AI、五章批次、重试和取消 | 服务、流式解析、路由、重试预算及错误处理测试通过；Electron 批次预览通过。供应商调用使用模拟响应，未验证真实付费生成。 |
| 事实/状态/期待账本 | 浏览器、共享规则和数据库测试通过，包括冲突处理、知识边界和项目隔离。 |
| 质检、人工定稿和变更单 | 固定基准、硬性问题拦截、审批版本保护、队列及审稿交互测试通过。 |
| 发布排期和 TXT/Markdown/DOCX 导出 | 排期事务及实际导出文件的集成测试通过；没有向番茄后台发布作品。 |
| 运营指标 CSV、复盘和实验 | 解析、统计、持久化及浏览器实验操作通过。 |
| 加密备份、恢复副本、健康检查 | 加密/解密、恢复、路径防护及故障注入通过；Electron 健康检查通过。 |
| 模型来源、角色路由和代理 | UI、路由、接口协议、代理隧道和错误处理测试通过；没有读取密钥内容或向云模型提交用户作品。 |
| 安全和隐私 | IPC 参数、出站校验、导航/权限限制、日志脱敏、研究隔离和人工审批相关测试通过。 |
| 软件更新、定时采集、自动备份调度 | 规则及设置测试通过；真实更新下载/安装和长时间定时运行未现场验证。 |
| 页面布局和无障碍 | 桌面 1440×900、移动 412×915 的现有场景通过；不是逐像素检查所有页面状态。 |

## 后续验收重点

1. 将已回归的最新修复更新到实际使用的程序，确认启动后补齐旧目录库结构。
2. 对真实模型完成一次候选生成、开书方案扩展和单章草稿生成，确认严格 schema 错误消失。
3. 使用实际安装路径验证立项草稿保存、章节保存及关闭重开后的持久化。

真实模型调用、完整自动更新安装和长期定时任务尚未完成现场验收，不应标为全部正常。
