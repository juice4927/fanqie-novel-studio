# 一次开书方案（ONE_CLICK_LAUNCH_PLAN）

状态：待确认。日期：2026-09-09。
目标：作者只选大方向，AI 一次产出世界观、人物、剧情、大纲；产出自带违规与抄袭检查。

## 1. 现状与不满的根源

### 1.1 现在的流程

1. `src/components/NewProjectModal.tsx` 打开就是"AI 从零开书"：先填**番茄分类 + 方向数量 + 灵感**，再在折叠的"高级选项"里做十几项决定（二级流派、开局形态、篇幅形态、视角、复合叙事类型、题材元素、主角身份、情绪基调、自定义方向、目标字数、单章字数、安全存稿线、更新节奏，外加榜单标签/机会/洞察包三组证据勾选）。
2. 点"生成三套方案" → `generateBookConcepts`（`electron/ai-service.ts:978`）只产出**概念卡**：书名、前提、开局机制、成长载体、升级阶梯等。
3. 作者在一堆文本卡片里**选定一套** → `createProjectFromConcept`（`electron/handlers/project-handlers.ts:347`）才调 `expandBookConcept`（`ai-service.ts:1056`）生成世界观、人物、势力、时间线。
4. 项目建好后 `startLaunchPack`（`project-handlers.ts:235`）在后台继续生成全书结构、全书粗纲、前 100 章章纲。
5. 作者在 `LaunchPackReview` 里复核，点"确认创作包"才解锁写作。

### 1.2 三个具体痛点

| 痛点 | 证据 |
| --- | --- |
| 作者被迫当策划 | 开书面板一次要求约 13 项定位决定 + 3 组证据勾选，而作者要的是"只选大方向" |
| 在信息最少时做最重的决定 | 第 3 步的"选方向"发生在世界观、人物、大纲**都还不存在**的时候；真正该看的成品在后面 |
| 合规与原创只有模型自评 | `src/shared/incubation-review.ts:326` 的"原创风险"直接读模型自己填的 `differentiation.originalityRisk`；`:339` 的"同质化"只和作者自己已有作品做 n-gram 比对；全流程没有任何平台违规检查 |

结论：要改的不是"多生成一点"，而是**把决定点从前面挪到后面**，并把检查补上。

## 2. 新流程总览

```
选大方向（1 个分类 + 最多 1 段话）
      ↓  一次点击
AI 一次产出：定位 → 概念 → 故事圣经（世界观/人物/势力/时间线）
              → 全书结构 → 全书粗纲 → 开局章纲 → 合规与原创检查
      ↓
复核完整成品（唯一确认点）→ 确认创作包 → 开始写作
```

作者只做两个决定：**选大方向**、**确认成品**。

## 3. 入口：只选大方向

### 3.1 必填与可选

| 项 | 默认 | 说明 |
| --- | --- | --- |
| 番茄分类 | 必选 | 决定写作基线规则，是唯一必须的结构性选择 |
| 一句话方向 / 边界 | 可留空 | 留空 = 完全交给 AI；写了就作为硬约束 |

其余全部由 AI 决定，并在产出里给出理由：目标字数、单章字数、二级流派、开局形态、篇幅形态、视角、复合叙事类型、题材元素、主角身份、情绪基调、更新节奏、安全存稿线。

### 3.2 高级选项与证据

- 高级选项继续保留，但**默认状态是"由 AI 决定"**；作者展开并改动过的字段才作为硬约束传下去，未改动的交给 AI。
- 证据（榜单标签、市场机会、脱敏洞察包）默认**自动取该分类的本地快照**，不再要求逐项勾选；本地没有快照就直接跳过，不阻塞生成。
- 方向数量开关：默认 **1 套**（AI 按分类 + 灵感决策）。想比较时可选"给我三个方向"，三套并排比较的能力保留在高级模式，但不再是必经步骤。

## 4. 一次生成：单动作，后台多批

点击"生成完整开书方案"后，后台按固定顺序编排，全部写进现有 `launchPack` 进度，可暂停、可续跑、失败可恢复：

| 阶段 | 复用/新增 | 产物 |
| --- | --- | --- |
| 定位决策 | 新增 | 从分类 + 灵感补齐定位卡，输出各项选择与理由 |
| 概念 | 复用 `generateBookConcepts`，数量固定 1 | 前提、开局机制、成长载体、升级阶梯、书名候选 |
| 故事圣经 | 复用 `expandBookConcept` | 主角弧光、关键关系、世界规则、主要势力、时间锚点、人物档案、地理与日常秩序、主线与伏笔、题材专属栏目 |
| 全书结构 | 复用 `generateLaunchPlanning("全书结构")` | 宏观阶段 + 分卷 |
| 全书粗纲 | 复用 `generateLaunchPlanning("全书粗纲")` | 每 10 章一条，覆盖到全书末章 |
| 开局章纲 | 复用 `generateLaunchPlanning("后续章纲")` | 前 100 章细纲与场景卡 |
| 合规与原创检查 | 新增 | 检查报告 + 阻断项 |

要点：

- 这一步**只产草稿**，不批准契约、不写正文，人工门禁不变。
- `LaunchPackProgress.phase`（`src/shared/types.ts:1040`）需要新增"定位决策""故事圣经""合规检查"三个取值，并保证每个阶段可幂等重入（中断后从已完成阶段继续）。
- 现在的"先选概念卡"从必经步骤变成**可选的比较模式**。默认路径下 AI 自己选一套继续展开。

## 5. 复核页：唯一确认点

在现有 `LaunchPackReview`（`src/components/LaunchPackReview.tsx`）上扩展，一次看完全部：

- **故事与世界**：已有契约字段展示（含 `genreSpecificSections` 的人物档案），补齐人物档案的阅读体验。
- **分层大纲**：宏观阶段 / 分卷 / 粗纲 / 细纲 / 场景卡，已有分页。
- **全书章纲**：已有。
- **新增：合规与原创**：检查报告，阻断项逐条人工确认并留痕。
- 每个板块支持**局部重生成**：只重做人物档案、只重做某一卷、只重做某段章纲，不用整包重跑。
- 底部一个"确认创作包"按钮 = 唯一门禁；确认时一次性完成契约批准与规划放行。

不满意时的出口：**换一个方向重开**（保留分类和灵感，重新生成一整套）、**局部调整**、或（开了三方向时）**一键切换备选方向**（切换会重跑下游阶段）。

## 6. 违规防线

分四层，全部是**本地纯函数**，放在 `src/shared/`，可被渲染进程和主进程复用：

| 层 | 检查内容 | 数据来源 |
| --- | --- | --- |
| L1 分类禁忌与毒点 | 分类 taboo、常见毒点、题材禁忌 | 复用 `fanqie-taxonomy` 的 `taboo`/`clicheTraps` 与 `genre-plugins` 的 `tabooBoundaries` |
| L2 平台敏感内容 | 涉政、涉黄、涉暴、涉毒、涉赌、迷信、未成年人相关、真实机构/公众人物/品牌、地域歧视等 | 新增 `src/shared/compliance-review.ts`，关键词 + 模式表，分级 阻断/警告/提示 |
| L3 书名与标签 | 极限词、违规符号、平台禁用词 | 同上，作用于 `titleOptions` 与 `suggestedTags` |
| L4 章节级 | 沿用现有质检硬门禁 | 开书阶段先查大纲层，写作阶段查正文 |

**边界必须说清楚**（写进界面文案，也写进报告）：

- 本地规则只能覆盖**可枚举的显性风险**，不能替代平台审核；番茄审核是黑盒。
- 定位是"发布前风险提示 + 人工确认留痕"，不承诺"不会违规"，也不做"一键过审"的暗示。
- 关键词表可维护、有测试覆盖；命中只是提示线索，不是判定结论。

## 7. 原创防线

### 7.1 生成端

- 现有提示词已禁止复述或模仿具体作品、避开热榜书名与独特设定（`ai-service.ts:978` 的 system 与 `baseUser`）。
- 加强：书名候选与主角名要过"与本地已有作品 + 常见套路模板"的相似度检查；`differentiation.against` 必须给出具体差异。

### 7.2 检查端

- **内部比对**：与作者已有作品、历史开书草稿做 n-gram 指纹比对，复用 `incubation-review.ts:61` 的 `ngramSimilarity`，把比对范围从"单次候选"扩到"跨草稿、跨项目"。
- **外部查重**：受隐私规则约束（AGENTS.md 规则 2：不上传全文或账本），**不能自动把设定和正文发到第三方**。做法是导出一份"自查清单"——书名候选、主角名、核心设定关键词、首章钩子摘要，全部是脱敏片段——由作者手动到平台或搜索引擎自查。默认不联网。
- **可选联网近似检索**：默认关闭，需作者显式开启，且只发脱敏片段，与"语义拆解只上传脱敏片段"保持一致。

### 7.3 边界必须说清楚

- 模型自评的 `originalityRisk` 是**自证**，不能当保证。
- 本地比对只覆盖"你库里的作品 + 可枚举的常见套路"，**不能证明与全网无重合**。
- 因此原创"高风险"一律是**阻断**，必须人工确认或换方向，确认留痕。

## 8. 改动清单

按可独立提交的阶段拆分：

**阶段 1 — 入口精简 + 一次编排（不改数据模型）**
- `src/components/NewProjectModal.tsx`：默认只留分类 + 一句话；高级选项改为"未改 = 交给 AI"。
- `electron/handlers/project-handlers.ts`：新增一次编排入口，把定位决策、概念、故事圣经、结构、粗纲、章纲串成一个后台任务，写进 `launchPack` 进度。
- `src/shared/types.ts`：`LaunchPackProgress.phase` 扩展；`IncubationPositioning` 允许"未决定"。
- 兼容：旧草稿（探索/定向/直达）继续可读；三案对比保留在高级模式。

**阶段 2 — 合规检查**
- 新增 `src/shared/compliance-review.ts` + 测试。
- `LaunchPackReview` 新增"合规与原创"视图与阻断项确认。
- 阻断项未确认时禁止"确认创作包"。

**阶段 3 — 原创检查**
- 新增 `src/shared/originality-review.ts`（跨项目指纹比对 + 自查清单导出）+ 测试。
- 生成端加强书名/主角名的相似度校验。

**阶段 4（可选）— 三方向比较模式**
- 把现有三案对比从默认路径改为高级模式。

## 9. 风险与代价

| 风险 | 应对 |
| --- | --- |
| 一次生成时间长、累计成本高（全书粗纲按 10 章/批，1200 章 = 120 批） | 单批成本已由 `CONTEXT_WINDOW_PLAN.md` 的预算裁剪控制；批次数不变。界面显示阶段进度，可暂停 |
| 长任务中断 | 复用现有 `launchPack` 进度与"已暂停/继续生成"机制；新阶段必须可幂等重入 |
| 门禁被稀释 | 确认点后移但**不减少**：契约批准、规划放行仍在"确认创作包"时一次性完成，且保留逐项编辑与留痕 |
| 合规检查误报/漏报 | 分级处理：阻断必须人工确认，警告/提示只提示；关键词表有测试；界面明确"不替代平台审核" |
| 旧数据兼容 | 旧草稿与旧项目继续可读；`phase` 缺省按现有逻辑回退 |

## 10. 验收标准

1. 从打开新建面板到看到完整方案，作者只需要：选 1 个分类、最多写 1 段话、点 1 次按钮。
2. 一次产出包含：故事圣经（主角弧光、关键关系 ≥2、世界规则 ≥2、势力 ≥2、时间锚点 ≥3、人物档案覆盖主角与主要对手且各有独立目标）、宏观阶段 ≥4、分卷 ≥3、全书粗纲覆盖到末章、开局章纲 100 章。
3. 合规报告覆盖全部阻断项，未逐条确认不能进入写作。
4. 原创报告给出风险等级与自查清单；高风险为阻断。
5. 中断后可从已完成阶段续跑，不重复生成已有产物。
6. `npm test`、`npm run build`、`npm run test:quality` 通过。

## 11. 明确不做

- 不做自动联网查重上传全文或设定。
- 不承诺"生成结果不会违规"或"一键过审"。
- 不取消任何人工确认环节；只把确认点从"盲选概念"挪到"复核成品"。
