# 让系统写出"好看"的小说：美学评测与反馈闭环方案

> 依据：`ANALYSIS.md` 的领域层与质量基准分析、`OPTIMIZATION_PLAN.md` 的工程基线。
> 核心立场：**"好看"的上限在人与模型，但系统可以为它装上可评测、可回归、可改进的仪表盘**——把追读节奏从玄学变成工程指标，同时明确不越权（不自动改稿、不设美学门禁）。

---

## 一、现状盘点：系统已有与缺失的评测

### 已有的两块评测（实测全绿）

| 评测 | 文件 | 测什么 | 本质 |
|---|---|---|---|
| 质量基准 | `src/shared/quality-benchmark.ts` + `quality-benchmark-corpus.ts` | 硬性问题召回率/准确率/误报控制 | **找错**（事实冲突、资源超支、重复机制） |
| 提示词回归 | `src/shared/prompt-evaluation.ts` + `prompt-regression-corpus.ts` | 结构/事实/重复/成本四维 | **防退化**（提示词改动不破坏输出格式） |

### 缺失的第三块：美学基准

系统里"好看"相关的资产全部是**输入侧引导**，没有**输出侧评测**：

- `prose-temperature.ts`：只做"低温文"预警，阈值判定，不产生分数；
- `aesthetic-profile.ts`：六维审美画像（叙事距离/情绪温度/文字肌理/对话风格/情绪表达/标志性手法）只编译成提示词文本，**没有对应评测规则**，作者无法知道输出是否贴合画像；
- `commercial-knowledge.ts` 的 CORE_LOOP 六步循环、`genre-plugins.ts` 的回报阶梯：只进 prompt，不进评测；
- 期待账本（`expectation-service.ts`）：记录"进入期待→兑现"，但**没有按章节核算"期待兑现率/兑现延迟"**；
- `metrics.ts`：运营数据（留存/追读/完读）已有导入与相关性分析，但**没有与章节美学特征挂钩**。

**结论：系统能可靠地防止"写崩"，但对"写得好不好看"既无测量、无回归、也无反馈，只有单方向的提示词祈祷。**

---

## 〇、平台锚定：本方案只服务一个标准——番茄数据模型下的"好看"

本系统面向番茄小说（免费阅读 + 广告分成 + 新书期数据推荐）。因此：

> **"好看"的定义 = 完读率、追读率、留存率等番茄运营指标，而非通用文学品味。**

三条硬约束（后续所有评测规则必须服从）：

1. **黄金标准是番茄高数据书**：美学语料优先来自番茄榜单书公开试读的前 10 章（系统已具备采集能力），做"高追读样本 vs 普通样本"的特征对照，替代设计者主观品味；主观语料只作补充。
2. **每个评测维度必须能映射到番茄漏斗指标**（见层 1 维度表的"番茄映射"列），最终由层 4 数据飞轮验证"维度分高 ⇔ 留存高"是否成立。
3. **防"文青陷阱"**：评测规则必须奖励"平实直白、短句、强冲突、利落节奏"，禁止奖励"辞藻华丽但拖沓"；语料中专门设置"番茄爆款风（平实但节奏好）为好例、华丽但拖沓为差例"的对照用例，防止系统朝读者不爱看的方向"优化"。

**附加收益——防 AI 味**：番茄对低质 AI 生成内容有限流风险。美学基准增加"AI 味检测"维度（重复句式率、情绪词单调度、结构模板化程度），在质检中心提示"本章 AI 味偏重"，既是质量工具也是平台合规工具。

---

## 二、方案总览：四层闭环

```
层1 美学评测闭环   → 可量化：npm run test:aesthetic 对固定语料打分，绑定提示词版本回归
层2 写作反馈闭环   → 可见化：定稿生成章节美学报告，质检中心看 30 章趋势（只提示不门禁）
层3 画像落地评测   → 可对齐：审美画像从提示词引导升级为可测的"风格漂移"检测
层4 数据飞轮       → 可校准：美学报告 × 运营留存数据的相关性分析，实证校准语料阈值
```

---

## 层 1：美学评测闭环（核心，先行）

### 1.1 新文件：`src/shared/aesthetic-benchmark.ts`

评估函数 `evaluateAestheticBenchmark(案例) → { 维度分, 总分, 违规明细 }`，对"章节正文 + 编译上下文"输出五个维度分（各 0–100）：

| 维度 | 测什么 | 可复用资产 | 番茄映射 |
|---|---|---|---|
| **钩子强度** | 开头 3 段是否含悬念/冲突/异常信息；末段是否埋新期待 | 期待账本、`context-compiler` 的章末推进 | 开篇钩子 ↔ 首章完读率；章末钩子 ↔ 追读率 |
| **期待-兑现** | 章首承接的期待是否在本章推进或兑现；兑现延迟是否在合理区间（1–3 章） | `ExpectationEntry` 结构（已有 linkedExpectationIds） | 兑现节奏 ↔ 三章留存 |
| **情绪回报** | 压力积累→回报兑现的节拍是否完整（CORE_LOOP 六步）；情绪转折点密度 | `commercial-knowledge.ts` 的 CORE_LOOP、`prose-temperature.ts` 情绪词表 | 回报密度 ↔ 追读/留存 |
| **节奏多样性** | 句长方差、段落长度分布、对话/叙述比例是否单调 | `context-compiler.ts` 的 authorStyle 统计逻辑（句长/段长/对话密度） | 单调性 ↔ 平均阅读时长；**番茄偏好短句直白，规则按爆款样本校准而非文学标准** |
| **低温检测升级** | 复用 `prose-temperature` 词表，从"预警"升级为连续计分 | `prose-temperature.ts`（情绪/感官/互动/克制词表） | 情绪温度 ↔ 阅读时长/留存 |
| **AI 味检测**（新增） | 重复句式率、情绪词单调度、结构模板化程度 | 无现成资产，需新建词表与规则 | 平台合规：番茄对低质 AI 内容限流 |

评分规则全部为**确定性规则 + 词表统计**，不调模型（与现有 quality-benchmark 一致，可离线回归）。

### 1.2 新文件：`src/shared/aesthetic-benchmark-corpus.ts`

绑定 `PROMPT_VERSION`（复用 `src/shared/prompt-version.ts`）的固定语料：

- **好例 3–5 个**：高钩子/期待兑现及时/回报节拍完整的章节（声明各维度预期分区间）；
- **差例 3–5 个**：平铺直叙开篇、期待悬置超过 3 章、情绪回报缺失的章节（声明各维度预期分区间）；
- **番茄对照例（必须）**：至少 1 组"平实直白但节奏利落的爆款风（好例）vs 辞藻华丽但拖沓（差例）"，防止"文青陷阱"（见第〇节硬约束 3）；
- **AI 味例（必须）**：至少 1 个重复句式/模板化结构样本（差例），校准 AI 味检测维度；
- 每个用例声明类别、预期区间、可追溯证据（与 `quality-benchmark-corpus.ts` 的既有格式对齐）。

### 1.3 测试与入口

- `tests/aesthetic-benchmark.test.ts`：好例全过、差例全不过、**禁止误杀好例**（对齐 quality-benchmark 的失败条件风格）；
- `package.json` 新增 `"test:aesthetic": "vitest run tests/aesthetic-benchmark.test.ts"`；
- 失败条件：平均分低于阈值、好例被误杀、任何用例预期区间偏差；
- 接入 CI `verify` 为**软门禁**（先报告后强制，避免未校准前卡发布）。

### 1.4 回归机制

- 修改任何写作/质检提示词或 bump `PROMPT_VERSION` 前必须跑 `npm run test:aesthetic`；
- 仿照 `compareQualityBenchmarkVersions`（quality-benchmark.ts:167-175）：新版本平均分较基线下降 > 2 分即判回归失败。

---

## 层 2：写作反馈闭环（定稿后可见化）

### 2.1 定稿生成"章节美学报告"

- 扩展 `src/shared/summaries.ts` 的 `prepareFinalizedChapterSummaries`：定稿时顺带计算美学维度（钩子/回报/节奏/低温），写入章节摘要的附加字段；
- 报告只做**记录与展示**，不参与任何状态机判定。

### 2.2 质检中心新增"美学"视图

- `QualityWorkspace.tsx` 增加 tab（或并入现有汇总视图）：按章展示美学分数、30 章趋势线（情绪曲线、爽点间隔、低温段高亮）；
- 设计原则：**只提示不门禁**——美学不进硬性问题判定（`assertChapterTransition` 等状态机一律不动），避免机械门禁压制风格多样性；
- 作者在写作台即可看到"上一章期待兑现情况"（数据已存在，只需可视化）。

---

## 层 3：审美画像落地为评测目标

### 3.1 问题

`aesthetic-profile.ts` 的六维画像目前只编译进提示词（`compileAestheticGuidance`），系统无法回答"这章符不符合作者声明的画像"。

### 3.2 方案

- 为每个维度定义 2–3 条**可测代理规则**，例如：
  - 情绪温度=「热烈」→ 情绪词密度应落在区间 [a, b]；
  - 叙事距离=「贴身」→ 对话/叙述比例与视角限定词检查；
  - 文字肌理=「明快」→ 句长中位数与从句密度上限；
- 评测时对照画像输出"风格漂移"提示（如"本章情绪温度偏冷，与画像「热烈」偏离"）；
- 漂移提示同样只出现在美学报告，不拦截。

---

## 层 4：数据飞轮（实证校准）

### 4.1 问题

美学语料的阈值目前是**设计者主观设定**，没有数据校准。

### 4.2 方案

- 复用 `metrics.ts` 的运营导入（留存/追读/完读，已有时间相关性分析框架，明确不宣称因果）；
- 新报告：每 10 章一期的**"风格-数据"对照**——该期各章美学维度分 × 同期留存/追读变化，输出相关性提示（沿用 `analyzeMetrics` 的"只报告时间相关性"措辞纪律）；
- 校准路径：积累 3 本以上作品后，用"高留存章 vs 低留存章"的美学分分布**人工复核**语料阈值（半自动：系统出统计，人做决定）；
- 明确边界：运营数据不自动修改契约/规划/正文（与 README 既有承诺一致）。

---

## 落地清单（按序执行）

| 步骤 | 内容 | 工作量 | 验证 |
|---|---|---|---|
| 1 | `aesthetic-benchmark.ts` + 语料（6–10 用例）+ `test:aesthetic` | 2–3 人日 | 好例/差例全按预期，`npm test` 不回归 |
| 2 | 接入 CI verify（软门禁）+ 回归比较函数 | 0.5 人日 | CI 绿 |
| 3 | `summaries.ts` 扩展：定稿生成美学字段 | 1 人日 | 新增单测；定稿流程回归 |
| 4 | `QualityWorkspace` 美学视图 + 30 章趋势 | 2 人日 | browser-workflow 测试覆盖 |
| 5 | 画像漂移检测（层 3） | 1–2 人日 | 单测覆盖六维规则 |
| 6 | 风格-数据对照报告（层 4） | 1–2 人日 | `analytics.test.ts` 扩展 |
| 7 | bump `PROMPT_VERSION`，全量回归（test / test:quality / test:aesthetic / test:scale） | 0.5 人日 | 全绿 |

总计约 **8–11 人日**，层 1 独立可交付（约 3 人日），后续各层可渐进接入。

---

## 明确不做的事（边界）

1. **不自动改稿**：美学报告只提示，AI 仍只产候选（延续系统"AI 不越权"原则）；
2. **美学不进硬性门禁**：不把"分数低"变成"不能定稿"，防止套路化机械门禁；
3. **不宣称美学分数 = 好看**：它是代理指标，最终判断权在作者；
4. **运营数据不自动改创作**：只做相关性报告（沿用 README 既有承诺）。

---

## 预期效果

- **作者侧**：每章定稿后看到钩子/回报/节奏仪表盘，30 章趋势可复盘"哪段节奏掉了"——把"感觉写得不好"变成"能定位到第几章的哪个维度"；
- **系统侧**：提示词改动有了美学维度的回归护栏（与硬性问题基准互补：一个防写崩，一个防写平）；
- **长期**：数据飞轮让语料阈值从主观走向实证，美学评测随作品积累越用越准。
